function colorcell_updated = interactive_color_correction(aligned_red, aligned_green, aligned_blue, calcium_norm, colorcell_initial, Coomasqx, Coomasqy)
    
    % =====================================================================
    % INITIALISATION
    % =====================================================================
    NCell_total = length(colorcell_initial);
    colorcell_updated = colorcell_initial(:); 
    
    % Dictionnaire des couleurs
    cmap_classes = [
        1 0 0;      % 1. Rouge
        0 1 0;      % 2. Vert
        0 0 1;      % 3. Bleu
        1 1 0;      % 4. Jaune
        1 0 1;      % 5. Magenta
        0 1 1;      % 6. Cyan
        1 1 1;      % 7. Blanc
        0.5 0.5 0.5 % 8. Noir (Gris pour l'interface)
    ];
    
    noms_classes = {'1. Rouge', '2. Vert', '3. Bleu', '4. Jaune', '5. Magenta', '6. Cyan', '7. Blanc', '8. Noir'};
    couleurs_txt = {'r', [0 0.6 0], 'b', [0.8 0.8 0], 'm', 'c', 'k', [0.4 0.4 0.4]};

    current_selected_class = 7; % Par défaut : Blanc
    
    % =====================================================================
    % FENÊTRE ET AXE
    % =====================================================================
    screenSize = get(0, 'ScreenSize');
    figWidth = min(1600, screenSize(3) - 50); 
    figHeight = min(1000, screenSize(4) - 80);
    fig = uifigure('Name', 'Correction Interactive des Couleurs', 'Position', [25 40 figWidth figHeight]);
    
    axWidth = figWidth - 250;  
    axHeight = figHeight - 40;
    
    ax = uiaxes(fig, 'Position', [20, 20, axWidth, axHeight]);
    h_img = imagesc(ax, cat(3, aligned_red, aligned_green, aligned_blue));
    
    title(ax, 'Cochez/Décochez les couleurs à droite | Cliquez sur une cellule pour la corriger', 'FontSize', 14);
    hold(ax, 'on');
    set(ax, 'YDir', 'reverse');
    axis(ax, 'off');
    axis(ax, 'image'); 
    
    % =====================================================================
    % DESSIN DES ROIs (Basé sur Coomasqx/y avec boundary)
    % =====================================================================
        % =====================================================================
    % DESSIN DES ROIs (Basé sur Coomasqx/y avec convhull exclusif)
    % =====================================================================
    h_rois = gobjects(NCell_total, 1);
    h_markers = gobjects(NCell_total, 1); % Marqueurs de correction
    
    for n = 1:NCell_total
        if n <= length(Coomasqx) && n <= length(Coomasqy)
            x_all = Coomasqx{n}(:); 
            y_all = Coomasqy{n}(:);
            
            if length(x_all) >= 3
                % On utilise UNIQUEMENT convhull pour éviter les polygones tordus (nœuds papillons)
                k = convhull(x_all, y_all);
                
                xs = x_all(k);
                ys = y_all(k);
                
                c_idx = colorcell_updated(n);
                if c_idx < 1 || c_idx > 8 || isnan(c_idx), c_idx = 8; end 
                
                % Dessin avec patch
                h_rois(n) = patch(ax, 'XData', xs, 'YData', ys, ...
                                 'FaceColor', cmap_classes(c_idx, :), 'FaceAlpha', 0.1, ...
                                 'EdgeColor', cmap_classes(c_idx, :), 'LineWidth', 1, ...
                                 'HitTest', 'off', 'PickableParts', 'none');
            end
        end
    end
    
    % =====================================================================
    % PANNEAU 1 : FORCER LA CLASSE (Boutons Radio)
    % =====================================================================
    pnlX = figWidth - 220;
    pnl_class = uipanel(fig, 'Position', [pnlX, figHeight-270, 200, 250], 'Title', 'Classe à appliquer', 'FontWeight', 'bold');
    bg = uibuttongroup(pnl_class, 'Position', [5 5 190 220], 'BorderType', 'none');
    bg.SelectionChangedFcn = @(bg,event) change_selected_class(event.NewValue.Text);
    
    rb = gobjects(8,1);
    btn_y = 190; spacing = 24;
    for c = 1:8
        rb(c) = uiradiobutton(bg, 'Position', [10 btn_y 150 22], 'Text', noms_classes{c}, ...
                              'FontColor', couleurs_txt{c}, 'FontWeight', 'bold');
        if c == current_selected_class
            rb(c).Value = 1;
        end
        btn_y = btn_y - spacing;
    end

    % =====================================================================
    % PANNEAU 2 : AFFICHER / MASQUER (Cases à cocher)
    % =====================================================================
    pnl_vis = uipanel(fig, 'Position', [pnlX, figHeight-550, 200, 260], 'Title', 'Afficher / Masquer', 'FontWeight', 'bold');
    cb_vis = gobjects(8,1);
    btn_y = 200;
    for c = 1:8
        cb_vis(c) = uicheckbox(pnl_vis, 'Position', [10 btn_y 150 22], 'Text', noms_classes{c}, ...
                               'Value', 1, 'FontColor', couleurs_txt{c}, 'FontWeight', 'bold');
        cb_vis(c).ValueChangedFcn = @(cb,event) update_visibility();
        btn_y = btn_y - spacing;
    end

    % =====================================================================
    % BOUTON VALIDER ET CALLBACKS
    % =====================================================================
    uibutton(fig, 'Position', [pnlX, figHeight-630, 200, 60], 'Text', 'VALIDER', ...
             'FontWeight', 'bold', 'FontSize', 16, 'ButtonPushedFcn', @(btn,event) valider_callback());
             
    fig.WindowButtonDownFcn = @(src, event) handle_image_click();
    
    app_is_running = true;
    while app_is_running && isgraphics(fig)
        uiwait(fig);
    end
    
    if isgraphics(fig)
        close(fig);
    end
    
    % --- Fonctions imbriquées ---
    function change_selected_class(txt)
        current_selected_class = str2double(txt(1));
    end

    function update_visibility()
        vis_states = zeros(8,1);
        for i = 1:8, vis_states(i) = cb_vis(i).Value; end
        
        for i = 1:NCell_total
            if isgraphics(h_rois(i))
                c_idx = colorcell_updated(i);
                if c_idx >= 1 && c_idx <= 8
                    if vis_states(c_idx) == 1
                        h_rois(i).Visible = 'on';
                        if isgraphics(h_markers(i)), h_markers(i).Visible = 'on'; end
                    else
                        h_rois(i).Visible = 'off';
                        if isgraphics(h_markers(i)), h_markers(i).Visible = 'off'; end
                    end
                end
            end
        end
    end

    function handle_image_click()
        cp = ax.CurrentPoint;
        x_click = cp(1,1); y_click = cp(1,2);
        xlims = ax.XLim; ylims = ax.YLim;
        
        if x_click >= xlims(1) && x_click <= xlims(2) && y_click >= ylims(1) && y_click <= ylims(2)
            for i = 1:NCell_total
                if isgraphics(h_rois(i)) && strcmp(h_rois(i).Visible, 'on')
                    x_data = h_rois(i).XData; 
                    y_data = h_rois(i).YData;
                    
                    if inpolygon(x_click, y_click, x_data, y_data)
                        colorcell_updated(i) = current_selected_class;
                        h_rois(i).FaceColor = cmap_classes(current_selected_class, :);
                        
                        if isgraphics(h_markers(i))
                            delete(h_markers(i));
                        end
                        h_markers(i) = plot(ax, mean(x_data), mean(y_data), 'w*', 'MarkerSize', 8, ...
                                            'HitTest', 'off', 'PickableParts', 'none');
                        break; 
                    end
                end
            end
        end
    end

    function valider_callback()
        app_is_running = false; 
        uiresume(fig);
    end
end
% function colorcell_updated = interactive_color_correction(aligned_red, aligned_green, aligned_blue, calcium_norm, ...
%                                                           colorcell_initial, outline_gcampx, outline_gcampy)
% 
%     % =====================================================================
%     % INITIALISATION
%     % =====================================================================
%     NCell_total = length(colorcell_initial);
%     colorcell_updated = colorcell_initial(:); 
% 
%     % Dictionnaire des couleurs
%     cmap_classes = [
%         1 0 0;      % 1. Rouge
%         0 1 0;      % 2. Vert
%         0 0 1;      % 3. Bleu
%         1 1 0;      % 4. Jaune
%         1 0 1;      % 5. Magenta
%         0 1 1;      % 6. Cyan
%         1 1 1;      % 7. Blanc
%         0.5 0.5 0.5 % 8. Noir (Gris pour l'interface)
%     ];
% 
%     noms_classes = {'1. Rouge', '2. Vert', '3. Bleu', '4. Jaune', '5. Magenta', '6. Cyan', '7. Blanc', '8. Noir'};
%     couleurs_txt = {'r', [0 0.6 0], 'b', [0.8 0.8 0], 'm', 'c', 'k', [0.4 0.4 0.4]};
% 
%     zero_img = zeros(size(aligned_red));
%     current_selected_class = 7; % Par défaut : Blanc
% 
%     % =====================================================================
%     % FENÊTRE ET AXE
%     % =====================================================================
%     screenSize = get(0, 'ScreenSize');
%     figWidth = min(1600, screenSize(3) - 50); 
%     figHeight = min(1000, screenSize(4) - 80);
%     fig = uifigure('Name', 'Correction Interactive des Couleurs', 'Position', [25 40 figWidth figHeight]);
% 
%     axWidth = figWidth - 250;  
%     axHeight = figHeight - 40;
% 
%     ax = uiaxes(fig, 'Position', [20, 20, axWidth, axHeight]);
%     h_img = imagesc(ax, cat(3, aligned_red, aligned_green, aligned_blue));
% 
%     title(ax, 'Cochez/Décochez les couleurs à droite | Cliquez sur une cellule pour la corriger', 'FontSize', 14);
%     hold(ax, 'on');
%     set(ax, 'YDir', 'reverse');
%     axis(ax, 'off');
%     axis(ax, 'normal'); 
%     set(ax, 'DataAspectRatioMode', 'auto');
%     set(ax, 'PlotBoxAspectRatioMode', 'auto');
% 
%     % =====================================================================
%     % DESSIN DES ROIs & MARQUEURS (Épaisseur 1)
%     % =====================================================================
%     h_rois = gobjects(NCell_total, 1);
%     h_markers = gobjects(NCell_total, 1); % Marqueurs blancs pour les modifs
% 
%     for n = 1:NCell_total
%         if n <= length(outline_gcampx) && n <= length(outline_gcampy)
%             x_brut = outline_gcampx{n};
%             y_brut = outline_gcampy{n};
%             if length(x_brut) >= 3
%                 sorted_indices = convhull(x_brut, y_brut);
%                 x_contour = x_brut(sorted_indices);
%                 y_contour = y_brut(sorted_indices);
% 
%                 c_idx = colorcell_updated(n);
%                 if c_idx < 1 || c_idx > 8 || isnan(c_idx), c_idx = 8; end 
% 
%                 % Épaisseur passée à 1
%                 h_rois(n) = plot(ax, x_contour, y_contour, 'Color', cmap_classes(c_idx, :), ...
%                                  'LineWidth', 1, 'HitTest', 'off', 'PickableParts', 'none');
%             end
%         end
%     end
% 
%     % =====================================================================
%     % PANNEAU 1 : FORCER LA CLASSE (Boutons Radio)
%     % =====================================================================
%     pnlX = figWidth - 220;
%     pnl_class = uipanel(fig, 'Position', [pnlX, figHeight-210, 200, 200], 'Title', 'Classe à appliquer', 'FontWeight', 'bold');
%     bg = uibuttongroup(pnl_class, 'Position', [5 5 190 170], 'BorderType', 'none');
%     bg.SelectionChangedFcn = @(bg,event) change_selected_class(event.NewValue.Text);
% 
%     rb = gobjects(8,1);
%     btn_y = 150; spacing = 21;
%     for c = 1:8
%         rb(c) = uiradiobutton(bg, 'Position', [10 btn_y 150 22], 'Text', noms_classes{c}, ...
%                               'FontColor', couleurs_txt{c}, 'FontWeight', 'bold');
%         if c == current_selected_class
%             rb(c).Value = 1;
%         end
%         btn_y = btn_y - spacing;
%     end
% 
%     % =====================================================================
%     % PANNEAU 2 : AFFICHER / MASQUER (Cases à cocher)
%     % =====================================================================
%     pnl_vis = uipanel(fig, 'Position', [pnlX, figHeight-430, 200, 210], 'Title', 'Afficher/Masquer ROIs', 'FontWeight', 'bold');
%     cb_vis = gobjects(8,1);
% 
%     btn_y = 160; spacing = 21;
%     for c = 1:8
%         cb_vis(c) = uicheckbox(pnl_vis, 'Position', [10 btn_y 160 22], 'Text', noms_classes{c}, ...
%                                'FontColor', couleurs_txt{c}, 'FontWeight', 'bold', 'Value', 1);
%         cb_vis(c).ValueChangedFcn = @(cb,event) update_roi_visibility();
%         btn_y = btn_y - spacing;
%     end
% 
%     % =====================================================================
%     % PANNEAU 3 : LUMINOSITÉ IMAGE
%     % =====================================================================
%     pnl_img = uipanel(fig, 'Position', [pnlX, figHeight-650, 200, 210], 'Title', 'Luminosité Image');
% 
%     cb_red = uicheckbox(pnl_img, 'Position', [10, 160, 100, 22], 'Text', 'Rouge', 'Value', 1);
%     sld_red = uislider(pnl_img, 'Position', [15, 140, 160, 3], 'Limits', [0 5], 'Value', 1);
%     cb_green = uicheckbox(pnl_img, 'Position', [10, 115, 100, 22], 'Text', 'Vert', 'Value', 1);
%     sld_green = uislider(pnl_img, 'Position', [15, 95, 160, 3], 'Limits', [0 5], 'Value', 1);
%     cb_blue = uicheckbox(pnl_img, 'Position', [10, 70, 100, 22], 'Text', 'Bleu', 'Value', 1);
%     sld_blue = uislider(pnl_img, 'Position', [15, 50, 160, 3], 'Limits', [0 5], 'Value', 1);
%     cb_ca = uicheckbox(pnl_img, 'Position', [10, 25, 100, 22], 'Text', 'GCaMP', 'Value', 0);
%     sld_ca = uislider(pnl_img, 'Position', [15, 5, 160, 3], 'Limits', [0 5], 'Value', 1);
% 
%     cb_red.ValueChangedFcn = @(cb,event) update_image_channels(); sld_red.ValueChangedFcn = @(sld,event) update_image_channels();
%     cb_green.ValueChangedFcn = @(cb,event) update_image_channels(); sld_green.ValueChangedFcn = @(sld,event) update_image_channels();
%     cb_blue.ValueChangedFcn = @(cb,event) update_image_channels(); sld_blue.ValueChangedFcn = @(sld,event) update_image_channels();
%     cb_ca.ValueChangedFcn = @(cb,event) update_image_channels(); sld_ca.ValueChangedFcn = @(sld,event) update_image_channels();
% 
%     % =====================================================================
%     % BOUTONS
%     % =====================================================================
%     uibutton(fig, 'Position', [pnlX, figHeight-720, 200, 50], 'Text', 'VALIDER', ...
%              'FontWeight', 'bold', 'FontSize', 16, 'ButtonPushedFcn', @(btn,event) valider_callback());
% 
%     % =====================================================================
%     % GESTION DES CLICS
%     % =====================================================================
%     fig.WindowButtonDownFcn = @(src, event) handle_image_click();
% 
%     app_is_running = true;
%     while app_is_running && isgraphics(fig)
%         uiwait(fig);
%     end
%     if isgraphics(fig)
%         close(fig);
%     end
% 
%     % =====================================================================
%     % FONCTIONS IMBRIQUÉES
%     % =====================================================================
%     function valider_callback()
%         app_is_running = false; 
%         uiresume(fig);
%     end
% 
%     function change_selected_class(text_label)
%         current_selected_class = str2double(text_label(1));
%     end
% 
%     function update_roi_visibility()
%         vis_states = [cb_vis(1).Value, cb_vis(2).Value, cb_vis(3).Value, cb_vis(4).Value, ...
%                       cb_vis(5).Value, cb_vis(6).Value, cb_vis(7).Value, cb_vis(8).Value];
% 
%         for i = 1:NCell_total
%             c_idx = colorcell_updated(i);
%             if c_idx < 1 || c_idx > 8 || isnan(c_idx), c_idx = 8; end
% 
%             is_vis = vis_states(c_idx);
% 
%             if isgraphics(h_rois(i))
%                 if is_vis, h_rois(i).Visible = 'on'; else, h_rois(i).Visible = 'off'; end
%             end
%             if isgraphics(h_markers(i))
%                 if is_vis, h_markers(i).Visible = 'on'; else, h_markers(i).Visible = 'off'; end
%             end
%         end
%     end
% 
%     function handle_image_click()
%         cp = ax.CurrentPoint;
%         x_click = cp(1,1); y_click = cp(1,2);
% 
%         xlims = ax.XLim; ylims = ax.YLim;
% 
%         if x_click >= xlims(1) && x_click <= xlims(2) && y_click >= ylims(1) && y_click <= ylims(2)
%             for i = 1:NCell_total
%                 if i <= length(outline_gcampx) && i <= length(outline_gcampy)
%                     x_brut = outline_gcampx{i}; y_brut = outline_gcampy{i};
%                     if length(x_brut) >= 3
%                         sorted_indices = convhull(x_brut, y_brut);
%                         x_lisse = x_brut(sorted_indices); y_lisse = y_brut(sorted_indices);
% 
%                         if inpolygon(x_click, y_click, x_lisse, y_lisse)
%                             % Mise à jour de la classe en mémoire
%                             colorcell_updated(i) = current_selected_class;
% 
%                             % Changement de couleur de la ligne
%                             if isgraphics(h_rois(i))
%                                 h_rois(i).Color = cmap_classes(current_selected_class, :);
%                             end
% 
%                             % Ajout du petit repère blanc (*) au centre pour signaler la modif
%                             if ~isgraphics(h_markers(i))
%                                 hold(ax, 'on');
%                                 h_markers(i) = plot(ax, mean(x_lisse), mean(y_lisse), '*w', ...
%                                                     'MarkerSize', 6, 'HitTest', 'off', 'PickableParts', 'none');
%                             end
% 
%                             % On met à jour l'affichage au cas où on vient d'assigner
%                             % une cellule à une classe qui est actuellement masquée
%                             update_roi_visibility();
% 
%                             break; 
%                         end
%                     end
%                 end
%             end
%         end
%     end
% 
%     function update_image_channels()
%         r_layer = zero_img; g_layer = zero_img; b_layer = zero_img;
%         if cb_red.Value == 1,   r_layer = aligned_red * sld_red.Value;       end
%         if cb_green.Value == 1, g_layer = aligned_green * sld_green.Value;   end
%         if cb_blue.Value == 1,  b_layer = aligned_blue * sld_blue.Value;     end
%         if cb_ca.Value == 1
%             ca_boosted = calcium_norm * sld_ca.Value;
%             r_layer = r_layer + ca_boosted; g_layer = g_layer + ca_boosted; b_layer = b_layer + ca_boosted;
%         end
%         r_layer(r_layer > 1) = 1; g_layer(g_layer > 1) = 1; b_layer(b_layer > 1) = 1;
%         h_img.CData = cat(3, r_layer, g_layer, b_layer);
%     end
% end